# SmartPACS (DicomCloud) - Production Docker Compose
# Usage: docker compose -f docker-compose.prod.yml up -d --build

name: smartpacs

services:
  traefik:
    # Pinned minor versions of Traefik bundle an old Docker client that this
    # host's Docker Engine (29.x) rejects (client requests API 1.24, daemon
    # requires 1.40+). DOCKER_API_VERSION is not honored by Traefik's docker
    # provider, so track latest instead of pinning to get an updated client.
    image: traefik:latest
    container_name: smartpacs-traefik
    restart: always
    command:
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --entrypoints.web.http.redirections.entrypoint.to=websecure
      - --entrypoints.web.http.redirections.entrypoint.scheme=https
      - --certificatesresolvers.letsencrypt.acme.httpchallenge=true
      - --certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web
      - --certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL}
      - --certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json
      - --api.dashboard=false
      - --api.insecure=false
      - --log.level=INFO
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik_letsencrypt:/letsencrypt
    networks:
      - smartpacs-net

  postgres:
    image: postgres:16-alpine
    container_name: smartpacs-postgres
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME:-dicomcloud}
      POSTGRES_USER: ${DB_USER:-dicomcloud}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    expose:
      - '5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-dicomcloud}']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - smartpacs-net

  redis:
    image: redis:7-alpine
    container_name: smartpacs-redis
    restart: always
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD}
      --appendonly yes
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    expose:
      - '6379'
    healthcheck:
      test: ['CMD-SHELL', 'redis-cli -a "$REDIS_PASSWORD" ping | grep -q PONG']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - smartpacs-net

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      target: production
    container_name: smartpacs-api
    restart: always
    env_file: .env.production
    environment:
      NODE_ENV: production
    expose:
      - '3001'
    volumes:
      - api_storage:/app/storage
      - api_logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:3001/api/v1/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - smartpacs-net
    labels:
      traefik.enable: 'true'
      traefik.docker.network: smartpacs-net
      traefik.http.routers.smartpacs-api.rule: (Host(`smartpacs.com.br`) || Host(`www.smartpacs.com.br`)) && (PathPrefix(`/api`) || PathPrefix(`/socket.io`) || PathPrefix(`/docs`) || PathPrefix(`/health`))
      traefik.http.routers.smartpacs-api.entrypoints: websecure
      traefik.http.routers.smartpacs-api.tls: 'true'
      traefik.http.routers.smartpacs-api.tls.certresolver: letsencrypt
      traefik.http.routers.smartpacs-api.priority: '10'
      traefik.http.services.smartpacs-api.loadbalancer.server.port: '3001'
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      target: production
      args:
        NEXT_PUBLIC_API_URL: ${API_URL}
        NEXT_PUBLIC_APP_URL: ${APP_URL}
    container_name: smartpacs-web
    restart: always
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${API_URL}
      NEXT_PUBLIC_APP_URL: ${APP_URL}
    expose:
      - '3000'
    depends_on:
      - api
    networks:
      - smartpacs-net
    labels:
      traefik.enable: 'true'
      traefik.docker.network: smartpacs-net
      traefik.http.routers.smartpacs-web.rule: Host(`smartpacs.com.br`) || Host(`www.smartpacs.com.br`)
      traefik.http.routers.smartpacs-web.entrypoints: websecure
      traefik.http.routers.smartpacs-web.tls: 'true'
      traefik.http.routers.smartpacs-web.tls.certresolver: letsencrypt
      traefik.http.routers.smartpacs-web.priority: '1'
      traefik.http.services.smartpacs-web.loadbalancer.server.port: '3000'

networks:
  smartpacs-net:
    name: smartpacs-net
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  api_storage:
  api_logs:
  traefik_letsencrypt:
