apiVersion: 1

providers:
  - name: SmartPACS
    orgId: 1
    folder: SmartPACS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
